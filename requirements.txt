# ── Core runtime ───────────────────────────────────────
torch>=2.2,<2.5
numpy>=1.26,<2.0
pandas>=2.1
scikit-learn>=1.4

# ── API + config (used in later phases, listed here so a single
#    `pip install -r requirements.txt` boots the whole project) ──
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6.0
python-dotenv>=1.0

# ── Persistence (Phase 4+) ────────────────────────────
SQLAlchemy>=2.0
aiosqlite>=0.19
alembic>=1.13

# ── Scheduling / async I/O (Phase 5+) ─────────────────
APScheduler>=3.10
httpx>=0.27
